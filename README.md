# Laser-Security-System-Arduino
An Arduino-based laser security alarm system using a digital LDR module, buzzer, push button reset, and I2C LCD display. The system detects laser beam interruption and triggers an alert with visual and audible indications.
